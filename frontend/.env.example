# In local dev, leave this unset — vite.config.js proxies /api to
# http://localhost:5000 automatically.
#
# In Vercel (frontend project settings -> Environment Variables), set this
# to your deployed backend's URL:
VITE_API_URL=https://your-backend-project.vercel.app/api/v1
